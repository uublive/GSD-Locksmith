Run the GSD team registry status command to show all active claims:

```bash
bash .gsd/locksmith/gsd-status.sh
```

Display the output as-is. If the command fails, check that `gh auth login` has been run and `.gsd/locksmith/config.json` exists.
